import { UsersRound } from 'lucide-react';
import { WorkspacePage } from '@/components/dashboard/workspace-page';
export default function Page() { return <WorkspacePage eyebrow="DESCOBERTA" title="Criadores" description="Descoberta e seleção dentro da rede Lucre." icon={UsersRound} emptyTitle="Mercado ainda não liberado" emptyDescription="A arquitetura está protegida por organização; o catálogo real entra após a fundação." />; }
